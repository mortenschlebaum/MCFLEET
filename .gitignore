# Dependencies — skal installeres med npm install (ikke committes)
mcfleet/node_modules/

# Build output
mcfleet/build/

# Lokale env-filer
.env
.env.local
.env.*.local

# Lokal sammenligning af zip-version (ikke i repo)
_compare_v100b/

# Cursor IDE-filer (lokale debug-logs, workspace-data osv.)
.cursor/

# Eget repo: github.com/mortenschlebaum/modulplan (ligger typisk ved siden af MCFLEET lokalt)
modulplan/
